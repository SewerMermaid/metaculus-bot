name: Probe Gemini OpenRouter access (paid)

on:
  workflow_dispatch:
    inputs:
      confirm_paid:
        description: Confirm two paid requests capped at 32 output tokens each
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  probe:
    if: ${{ inputs.confirm_paid }}
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Send two capped prompts
        env:
          OPENROUTER_KEY: ${{ secrets.OAI_ANTH_OPENROUTER_KEY }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          import sys
          import urllib.error
          import urllib.request

          CHAT_URL = "https://openrouter.ai/api/v1/chat/completions"
          MODELS = (
              "google/gemini-3.1-pro-preview",
              "google/gemini-3-flash-preview",
          )
          PROMPT = "Reply with exactly: OK"
          MAX_TOKENS = 32


          def safe_text(value: object, api_key: str) -> str:
              text = value if isinstance(value, str) else json.dumps(value, ensure_ascii=False)
              return text.replace(api_key, "[REDACTED]").replace("`", "'")


          def probe(model: str, api_key: str) -> tuple[bool, list[str]]:
              payload = {
                  "model": model,
                  "messages": [{"role": "user", "content": PROMPT}],
                  "max_tokens": MAX_TOKENS,
              }
              request = urllib.request.Request(
                  CHAT_URL,
                  data=json.dumps(payload).encode("utf-8"),
                  headers={
                      "Authorization": f"Bearer {api_key}",
                      "Content-Type": "application/json",
                      "Accept": "application/json",
                  },
                  method="POST",
              )

              try:
                  with urllib.request.urlopen(request, timeout=60) as response:
                      result = json.load(response)
                      status = response.status
              except urllib.error.HTTPError as exc:
                  body = exc.read().decode("utf-8", errors="replace")
                  try:
                      parsed = json.loads(body)
                      detail = parsed.get("error", {}).get("message", body)
                  except json.JSONDecodeError:
                      detail = body
                  return False, [
                      f"## `{model}`",
                      "",
                      f"- Result: failed (HTTP {exc.code})",
                      f"- Error: `{safe_text(detail, api_key)}`",
                      "",
                  ]
              except urllib.error.URLError as exc:
                  return False, [
                      f"## `{model}`",
                      "",
                      "- Result: failed (request error)",
                      f"- Error: `{safe_text(exc.reason, api_key)}`",
                      "",
                  ]

              choices = result.get("choices", [])
              message = choices[0].get("message", {}) if choices else {}
              content = message.get("content", "")
              usage = result.get("usage", {})
              lines = [
                  f"## `{model}`",
                  "",
                  f"- Result: success (HTTP {status})",
                  f"- Response: `{safe_text(content, api_key)}`",
                  f"- Prompt tokens: {usage.get('prompt_tokens', 'n/a')}",
                  f"- Completion tokens: {usage.get('completion_tokens', 'n/a')}",
                  f"- Total tokens: {usage.get('total_tokens', 'n/a')}",
                  "",
              ]
              return True, lines


          api_key = os.environ.get("OPENROUTER_KEY", "")
          if not api_key:
              raise SystemExit("OAI_ANTH_OPENROUTER_KEY is not configured")

          summary_lines = [
              "# Gemini donated-key smoke test",
              "",
              f"Prompt: `{PROMPT}`",
              f"Output cap: {MAX_TOKENS} tokens per model",
              "",
          ]
          failed = False
          for model in MODELS:
              succeeded, lines = probe(model, api_key)
              summary_lines.extend(lines)
              failed = failed or not succeeded

          summary = "\n".join(summary_lines)
          print(summary)
          with open(os.environ["GITHUB_STEP_SUMMARY"], "a", encoding="utf-8") as summary_file:
              summary_file.write(summary + "\n")

          if failed:
              sys.exit(1)
          PY
